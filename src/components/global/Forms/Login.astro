---
const handleLogin = () => {
  console.log("Login");
};

const button = () => (
  <button
    class="bg-[#005c53] text-white p-4 rounded-md w-full focus:outline-none"
    onClick={handleLogin}
  >
    Login
  </button>
);
---


    <div class="w-1/2 h-full bg-[#ffffff] flex flex-col p-10 px-20 justify-between">
      <h1 class="text-base text-[#005c53] font-regular">S.A.E MANAGEMENT</h1>

      <div class="w-full flex flex-col">
        <div class="text-[#005c53] flex flex-col mt-10">
          <h3 class="text-2xl font-semibold mb-4">Login</h3>
          <p class="text-sm mb-2">Bienvenido! Por favor, ingrese sus datos:</p>
        </div>

        <div class="w-full flex flex-col">
          <input type="text" placeholder="Usuario" class="border-b-2 border-[#005c53] p-4 mb-4 focus:outline-none" />
          <input type="password" placeholder="Contraseña" class="border-b-2 border-[#005c53] p-4 mb-4 focus:outline-none" />
        </div>

        <div class="mb-4">
          <input type="checkbox" class="mr-2 bg-[#dbf227]" id="remember-me" checked />
          <label class="font-normal" for="remember-me">
            Remember me
          </label>
        </div>
        <button>{button}</button>
      </div>

      <div class="w-full flex items-center justify-center">
        <p class="text-sm font-normal text-[#DBF227]">
          If you need an account. <span class="font-semibold underline underline-offset-2 cursor-pointer">Contact Us</span>
        </p>
      </div>
    </div>
  );
